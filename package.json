{
  "name": "root",
  "private": true,
  "scripts": {
    "publish": "lerna version --conventional-commits --no-private"
  },
  "workspaces": [
    "packages/*",
    "example-app"
  ],
  "devDependencies": {
    "lerna": "^6.3.0",
    "prettier": "^2.8.1"
  }
}
